range(1,6) = cria números de 1 até 5, list() = transforma em lista
x[1] = pega o elemento da posição 1
append(10) = adiciona no final da lista
del(x[1]) = remove pelo índice, remove("banana") = remove pelo valor
len(x) = conta quantos elementos existem
7 in x = verifica se 7 está na lista
x + y = junta duas listas
reverse() = inverte a lista original, [::-1] = cria uma lista invertida
count(2) = conta quantas vezes o 2 aparece
sum(x) = soma todos os valores
set(x) = remove duplicados (mas perde a ordem)
for + if = percorre e encontra maior/menor
[n**2 for n in ...] = cria lista com valores modificados
if n % 2 != 0 = pega números ímpares
x[-n:] = pega os últimos n elementos, x[:-n] = pega o resto, + = junta (rotação)
if n in y = pega elementos em comum
for y in x for n in y = achata lista de listas
Merge Sort = divide, ordena e junta
Kadane = maior soma contínua
Recursão = função chama ela mesma, permutação = todas as ordens possíveis da lista
